# 1337tools v4.66

Reference wordmark correction.

- removed the yellow diamond from the site wordmark
- restored the existing positions of `337` and `tools`
- only the digit `1` is visually nudged toward `337`
- separate nudge values are used for main/header and credits to match the supplied reference
- fonts are unchanged
